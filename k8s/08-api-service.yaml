# NodePort : accessible depuis l'extérieur du cluster, ex: minikube service api-svc --url
apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: elearning
spec:
  type: NodePort
  selector:
    app: api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
